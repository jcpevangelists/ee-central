div(ng-controller="ProjectsController", class="javaee-all-projects")
    include header-projects
    div
        section
            article(ng-repeat="project in projects")
                .javaee-project-icon
                    a(href="projects/{{project.name}}")
                        img(ng-src="data:image/png;base64,{{project.icon}}")
                .javaee-project-short-description
                    a(href="projects/{{project.name}}")
                        h3 {{project.friendlyName ? project.friendlyName : project.name}}
                        p {{project.shortDescription}}
                .javaee-project-buttons
                    div
                    div
                        a(href="projects/{{project.name}}") check it out
                        .javaee-project-share
                            h5 share it
                            div
                                i(javaee-share-project="twitter")
                                    i.fa.fa-twitter
                                i(javaee-share-project="facebook")
                                    i.fa.fa-facebook



include footer
